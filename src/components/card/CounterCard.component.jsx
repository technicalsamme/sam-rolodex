import React from "react"

class CounterCard extends React.Component {
    constructor(monster) {
        super();
    
      }

    render(){return <div>

<img alt="monster" src={`https://robohash.org/${props.monster.id}?set=set3&size=180x180`}/>
    <h1>{this.state.mon.name}</h1>
    </div>}
    
}